@EnableEurekaServer

@EnableAdminServer

@EnableDiscoveryClient

@EnableDiscoveryClient
@EnableFeignClients


@Autowired
	private TeluskoFeignClient feignClient;

	public String getCouerseInfo() {
//		RestTemplate restTemplate = new RestTemplate();
//		ResponseEntity<String> response = restTemplate.getForEntity("http://localhost:8582/course-info", String.class);
//		return response.getBody() + " Comming from another rest api";
		ResponseEntity<String> response = feignClient.getCourseInfo();
		return response.getBody() + " Comming from another rest api";
	}




import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;

@FeignClient(name="MICRO3RESTAPI")
public interface TeluskoFeignClient {

	@GetMapping("/course-info")
	public ResponseEntity<String> getCourseInfo();
}
-------------------------------------------------------------------------------------------
Day2

Spring Boot Starter Web: RESTful APIs banane ke liye.

Spring Boot Starter Actuator: App monitoring aur health check ke liye.

Micrometer Tracing Bridge Brave: Traces generate karne ke liye (Spring Boot 3 standard).

Zipkin Reporter Brave: Traces ko aapke Zipkin server tak bhejne ke liye.

Spring Cloud Starter Netflix Eureka Client: Service discovery registration ke liye.

Spring Cloud Starter OpenFeign: Inter-service communication ke liye.

Spring Cloud Starter Config: (Config Client) External configuration fetch karne ke liye.

Spring Cloud Config Server: (Config Server) Centralized configuration manage karne ke liye.

Spring Boot Admin Starter Client: Monitoring dashboard se connect karne ke liye.

Spring Boot DevTools: Fast development aur auto-restart ke liye.

Spring Boot Starter Test & Micrometer Tracing Test: Testing framework ke liye.
